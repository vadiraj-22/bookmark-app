# Supabase Configuration
# Get these values from: Supabase Dashboard → Project Settings → API

# Your Supabase project URL
# Format: https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Your Supabase anonymous (public) key
# This key is safe to use in the browser because Row Level Security protects your data
# Format: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit this file to version control (it's in .gitignore)
# 2. NEVER use the service_role key in client-side code
# 3. The anon key is safe to expose because RLS protects your data
# 4. For production, set these in your Vercel environment variables
